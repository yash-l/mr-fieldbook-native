# MR FieldFlow v19 — update an existing local project
rm -rf ~/MR-FieldFlow-v19
unzip -o MR-FieldFlow-v19-Offline-Source.zip -d ~

# If you already have a Git checkout, copy the v19 source into it:
cd /path/to/your/existing/repo
cp -a ~/MR-FieldFlow-v19/. .

# Validate JS before pushing
node --check app/src/main/assets/web/app.js

# Commit / push; GitHub Actions builds the APK
git add .
git commit -m "MR FieldFlow v19 workflow redesign and stability hardening"
git push
